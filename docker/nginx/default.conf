server {
    listen 8000;

    sendfile on;

    default_type application/octet-stream;

    root /usr/share/nginx/html;

    gzip_static       on;

    location / {
        try_files $uri $uri/ /index.html =404;
    }
}
