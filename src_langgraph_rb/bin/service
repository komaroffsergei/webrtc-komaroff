#!/usr/bin/env ruby
# frozen_string_literal: true

root = File.expand_path("..", __dir__)
$LOAD_PATH.unshift(File.join(root, "lib"))

require "src_langgraph_rb"

settings = SrcLanggraphRb::Runtime::Settings.from_env
service = SrcLanggraphRb::Runtime::Service.new(settings: settings)
service.run
